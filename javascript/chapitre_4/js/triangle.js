var ligne = ""; // on creer une variable string vide
for (var i = 1; i < 8; i++) { // on lance le compteur qui ira jusqua 7 (7 lignes)
    ligne += "#"; // on incremente la variable string 'ligne' d'un caractere '#' a chaque tour de boucle !
    console.log(ligne); // et on affiche cette variable ligne incrementée !
}
